# Progress Log

## 2026-03-15
- phase 1 complete: replaced xterm.js deps with ghostty-web@0.4.0 in package.json, ran bun install (6 packages removed, ghostty-web added)
- phase 2 (partial): created scripts/bundle-ghostty.ts — bundles ghostty-web UMD (with inlined WASM) into public/ghostty-web.bundle.js (625KB). Exposes xterm-compatible globals (Terminal, FitAddon.FitAddon) + no-op stubs for SearchAddon/Unicode11Addon/WebglAddon. window.ghosttyReady promise gates WASM init.
- phase 3: updated gen-assets.ts to invoke bundle-ghostty.ts instead of copying xterm assets. Removed 7 stale xterm files from public/. public/ now contains 4 files (ghostty-web.bundle.js, index.html, manifest.json, wolfpack-icon.svg). All 728 tests pass.
- phase 4: removed dead xterm <link>/<script> tags from index.html (xterm.css, xterm.min.js, 5 addon scripts). Replaced with single <script src="/ghostty-web.bundle.js"> tag. Regenerated public-assets.ts. All 728 tests pass.
- phase 5: added WASM init gate — mount() now awaits window.ghosttyReady before creating Terminal. initDesktopTerminal and mountGridController made async; renderGridCells collects mount promises and defers connect until all resolve. Stale-disposal guard added to initDesktopTerminal. All 728 tests pass.
- phase 6: removed Unicode11Addon — ghostty-web does unicode 15.1 natively. Removed from index.html (try/catch block + activeVersion setter), bundle-ghostty.ts (stub + comment), regenerated bundle. All 728 tests pass.
- phase 7: removed WebglAddon loading — ghostty handles its own rendering. Removed loadAddon call from index.html, removed WebglAddon stub + dead NoopAddon from bundle-ghostty.ts, regenerated bundle. All 728 tests pass.
- phase 8: updated FitAddon usage — exposed GhosttyWeb.FitAddon directly (no xterm-compat wrapper), changed `new FitAddon.FitAddon()` → `new FitAddon()` in index.html, regenerated bundle. All 728 tests pass.
- phase 9: handled search addon gap (option A — disable desktop search). Removed SearchAddon no-op stub from bundle, removed all searchAddon creation/loading/dispose/getter from index.html, desktop search shows "n/a" (mobile capture-pane search still works), removed xterm-find-result CSS, removed allowProposedApi. Regenerated bundle (624.5KB). All 728 tests pass.
- phase 10: updated xterm CSS references for ghostty-web. ghostty-web renders via `<canvas>` (no `.xterm` wrapper div), so changed all 7 `.xterm` CSS selectors in index.html to target `canvas` instead. Updated all stale xterm.js comments in index.html and websocket.ts to reference ghostty-web. Renamed `createXtermInstance` → `createTerminalInstance`. Only remaining `xterm` is `TERM: "xterm-256color"` env var (correct). All 728 tests pass.
- phase 11: tested `term.buffer.active` access for scroll position tracking. Extracted pure functions from index.html into `src/terminal-buffer.ts` (captureScrollState, scrollTargetAfterResize, serializeBufferTail) with typed TerminalBuffer interface documenting the ghostty-web API surface. 19 unit tests covering: at-bottom detection, scrolled-up state, empty terminal, resize clamping, buffer tail serialization (including null/sparse lines), and full scroll-preserve round-trips.
- phase 12 (verify: desktop single terminal): open, type, scroll, copy/paste verification. Added 42 tests across 2 files:
  - `tests/integration/desktop-terminal.test.ts` (18 tests): PTY WS attach handshake (attach → attach_ack ordering, skipPrefill variants, duplicate attach dedup, spawn failure → 4001 close), binary stdin forwarding (pre-attach drops, during-spawn safety, oversized frame drop, interleaved binary+JSON), resize handling (backward compat spawn via resize, latestRequestedSize tracking, rapid resize dedup, extreme value safety), session lifecycle (teardown on close, reconnect after failure, full lifecycle, rapid cycles).
  - `tests/unit/desktop-terminal-logic.test.ts` (24 tests): copy key handler for ghostty-web canvas (Cmd+C/Ctrl+C interception with/without selection, keyup pass-through, non-copy keys, both modifiers), binary encoding via charCodeAt & 0xff (ASCII, empty, single-byte, multi-byte truncation, control chars, CSI escapes), stdin forwarding guard (canAcceptInput gating, dynamic state), stdin TextEncoder encoding (ASCII, newline, CR, tab, escapes, empty, UTF-8 multi-byte).
  All 789 tests pass.
- phase 13 (verify: desktop grid): 2-6 cells, focus switching, stdin guard verification. Added 32 tests across 2 files:
  - `tests/integration/desktop-grid.test.ts` (23 tests): multi-session concurrency (2/4/6 simultaneous PTY WS connections with independent entries and attach_ack), session isolation (close one doesn't affect others, spawn failure independence, binary stdin isolation, rapid connect/disconnect while another stays connected, independent spawn counters), reset=1 grid remount (teardown+fresh entry, attach_ack on fresh, no cross-session impact, works without prior entry), per-cell resize independence (different dims per cell, resize on one doesn't trigger spawn on another, rapid resizes on multiple cells), stdin guard binary isolation (per-session binary frames, oversized frame isolation, interleaved JSON+binary across sessions), lifecycle (close middle of 3, reverse-order close, reconnect gets fresh entry).
  - `tests/unit/grid-logic.test.ts` (+9 tests): focus switching stdin guard across grid transitions (exactly-one-accepts-input invariant for 4 cells, all-can-resize for 6 cells, disconnected blocks both, focus change old/new swap, remove-adjusts-focus+gate, add-focuses-new-cell, exit-grid-restores-default), 5-cell and 6-cell complete arrow nav boundary checks.
  All 821 tests pass.
- reconnect: close/reopen websocket, hydration prefill. Removed `_skipPrefillNextAttach = wasReconnect` from createPtySocketClient — auto-reconnect now requests server prefill instead of skipping it. Added reconnect hydration to createPtyTerminalController onOpen: on reconnect, terminal is reset, hydration counters cleared, hydration controller restarted, and hydrating CSS applied so prefill data renders cleanly onto a fresh terminal. Grid cells with skipInitialPrefill correctly skip rehydration on manual retry (new ptyClient) but still rehydrate on auto-reconnect (same ptyClient). 21 unit tests in tests/unit/reconnect-hydration.test.ts covering shouldRehydrate decision matrix, skipPrefill lifecycle, and rehydration action simulation.
  All 842 tests pass.
- take-control flow: viewer conflict + recovery. Extracted pure take-control state machine into src/take-control-logic.ts (classifyDisconnect, handleViewerConflict, handleControlGranted, handleDisplaced, prepareAutoTakeControl). Added 47 tests across 2 files:
  - tests/integration/take-control.test.ts (20 tests): control_granted delivery (received after take_control, arrives before old viewer close), entry promotion (new entry created, pendingViewer nulled), re-attach after takeover (attach_ack on promoted entry, spawn counter resets), old PTY teardown (alive=false, mock proc kill verified, INV-5 old close handler safety), pending viewer cleanup (disconnect clears pendingViewer, third viewer displaces second then takes control), multi-hop chain A→B→C (three successive takeovers, displaced viewer reconnects and takes back control), pending message filtering (non-take_control JSON ignored, binary ignored, malformed JSON safe), cross-session isolation (takeover on one session doesn't affect another), edge cases (take_control after active viewer disconnected, close immediately after granted, rapid take-control cycles don't leak).
  - tests/unit/take-control-logic.test.ts (27 tests): close code constants, initial state, handleViewerConflict (show-overlay default, auto-take-control one-shot), handleControlGranted (clears both flags), classifyDisconnect (4002→displaced, 4001→session-ended, 1000+"pty exited"→pty-exited, all others→reconnect), handleTakeControlClick (connected→send, disconnected→reconnect-with-auto), handleDisplaced/prepareAutoTakeControl state transitions, full state machine sequences (normal flow, displaced recovery, double displacement, session-ended exclusion, reconnect classification).
  All 889 tests pass.
