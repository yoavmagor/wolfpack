# Progress Log

## Phase 1a: Imports — DONE (already present)
- `existsSync` and `spawn` were already imported in serve.ts

## Phase 1b: Ralph helpers — DONE (already present)
- `RALPH_BIN`, `RalphStatus`, `parseRalphLog()`, `findRalphPid()`, `scanRalphLoops()` all exist

## Phase 1c: API routes — DONE
- Added 4 endpoints to routes object in serve.ts:
  - `GET /api/ralph` — lists all ralph loops via scanRalphLoops()
  - `GET /api/ralph/log?project=X` — returns last 500 lines of .ralph.log
  - `POST /api/ralph/start` — spawns ralph detached with validation + conflict check
  - `POST /api/ralph/cancel` — finds PID via findRalphPid, kills process + group

## Phase 2a: CSS — DONE
- Added all ralph loop CSS styles to `public/index.html` before `</style>` (lines 588-755)
- Styles: `.ralph-card`, `.ralph-card-header`, `.ralph-card-name`, `.ralph-progress`, `.ralph-bar`/`.ralph-bar-fill` (with `.running`/`.done`/`.failed` variants), `.ralph-iter`, `.ralph-status` (with state variants), `.dot.purple` (pulsing), `.ralph-last-output`, `.ralph-detail-header`, `.ralph-log`, `.ralph-actions`, `.ralph-start-form`, `.ralph-launch-btn`, `.ralph-empty`, `.ralph-nav-btn`
- Purple accent (#9b59b6) used for ralph branding to distinguish from main green theme
- Matches existing design patterns: dark bg, 2px border-radius, monospace fonts, tap-highlight-color transparent

## Phase 2b: HTML views — DONE
- Added 3 new views after terminal-view closing `</div>` (lines 878-919):
  1. `ralph-view` — list container (`#ralph-list`) + "Start Ralph Loop" button
  2. `ralph-detail-view` — detail header, scrollable `<pre>` log, action buttons container
  3. `ralph-start-view` — form with project `<select>`, iterations number input (1-50, default 5), plan file text input, launch button
- All views follow existing pattern: `<div id="X-view" class="view">` hidden by default
- Regenerated public-assets.ts via `bun run scripts/gen-assets.ts`

## Phase 2c: Navigation — DONE
- Added 🥋 button (`ralph-nav-btn`, id=`ralph-btn`) to `header-right` div before search button
- Button hidden by default (`style="display:none"`), shown only on sessions view via `showView()`
- Added 3 new ralph state vars: `ralphRefreshTimer`, `currentRalphProject`, `ralphLogPollTimer`
- Updated `showView()` with:
  - Timer cleanup for `ralphRefreshTimer` and `ralphLogPollTimer` at top of function
  - `ralph` case: back→sessions, load loops, auto-refresh every 3s
  - `ralph-detail` case: back→ralph, refresh detail + poll log every 2s
  - `ralph-start` case: back→ralph, populate form
  - Ralph button visibility toggled per view (shown on sessions only)
- Regenerated `public-assets.ts` via `bun run scripts/gen-assets.ts`

## Phase 2d: JavaScript functions — DONE
- Added 7 functions to `public/index.html` (lines 1120-1276) before `// ── Sessions ──`:
  1. `loadRalphLoops()` — fetches `/api/ralph`, renders sorted card list (active-first), progress bars, status badges
  2. `openRalphDetail(project)` — sets `currentRalphProject`, navigates to detail view
  3. `refreshRalphDetail()` — fetches loop status + log, renders header/progress/actions, auto-scrolls log, stops polling when loop finishes
  4. `cancelRalph()` — confirm dialog → POST `/api/ralph/cancel`, refreshes detail
  5. `loadRalphStartForm()` — fetches `/api/projects`, populates project `<select>` dropdown
  6. `startRalph()` — reads form inputs, POST `/api/ralph/start`, navigates to ralph list on success
  7. `restartRalph()` — navigates to start form, pre-selects current project after 300ms delay
- Regenerated `public-assets.ts` via `bun run scripts/gen-assets.ts`

## Phase 3: Verification — DONE
- Full audit of serve.ts confirmed all backend components present:
  - RALPH_BIN, RalphStatus interface, parseRalphLog(), findRalphPid(), scanRalphLoops()
  - All 4 API routes: GET /api/ralph, GET /api/ralph/log, POST /api/ralph/start, POST /api/ralph/cancel
- Full audit of public/index.html confirmed all frontend components present:
  - All CSS styles (ralph-card, ralph-bar, ralph-status, dot.purple, etc.)
  - All 3 views (ralph-view, ralph-detail-view, ralph-start-view)
  - Navigation button (ralph-btn) with showView() integration
  - All 7 JS functions (loadRalphLoops, openRalphDetail, refreshRalphDetail, cancelRalph, loadRalphStartForm, startRalph, restartRalph)
- public-assets.ts is current (regenerated during Phase 2)
- No TypeScript errors (bun transpiles cleanly)

## ALL PHASES COMPLETE

## Security Fix 1: Project name validation — DONE
- Added `isValidProjectName()` helper at line 307 of serve.ts
- Rejects `.` and `..` as project names (directory traversal prevention)
- Replaced all 4 inline regex checks with the helper:
  - POST /api/create (line 473)
  - GET /api/ralph/log (line 700)
  - POST /api/ralph/start (line 724)
  - POST /api/ralph/cancel (line 766)
- Transpiles cleanly, no errors

## Security Fix 2: Tighten pgrep match — DONE
- Changed `pgrep -f "bin/ralph"` to `pgrep -f RALPH_BIN` in findRalphPid() (line 313)
- RALPH_BIN resolves to full absolute path (e.g. `/Users/home/bin/ralph`)
- Prevents matching unrelated processes that happen to contain "bin/ralph" in cmdline
- Transpiles cleanly, no errors

## Security Fix 3: Fix lsof substring match — DONE
- Changed `lsofOut.includes(projectDir)` to exact match via lsof `n` field parsing (line 324-325)
- Parses lsof output lines, finds the one starting with `n` (cwd field), slices off prefix, compares exactly
- Prevents `/Dev/foo` from matching `/Dev/foobar` in macOS cwd detection
- Transpiles cleanly, no errors

## Security Fix 4: Sanitize planFile input — DONE
- Added validation for `planFile` in POST /api/ralph/start (line 751-755)
- Rejects planFile containing `/`, `\\`, or equal to `.` or `..`
- Returns 400 with "invalid plan file name" if validation fails
- Prevents path traversal via planFile argument to ralph spawn
- Transpiles cleanly, no errors

## Security Fix 5: Add pid > 1 guard to cancel endpoint — DONE
- Changed `if (!pid)` to `if (!pid || pid <= 1)` in POST /api/ralph/cancel (line 777)
- Prevents `process.kill(-pid)` from killing init/launchd if findRalphPid returned pid=1
- Defensive guard — unlikely path but catastrophic if hit
- Transpiles cleanly, no errors

## Verification — DONE
- `bun run serve.ts` starts successfully on port 18790 (no compile errors)
- curl test: POST /api/ralph/start with project=".." → 400 `{"error":"invalid project name"}` ✓
- curl test: POST /api/ralph/start with planFile="../../../etc/passwd" → 400 `{"error":"invalid plan file name"}` ✓
- curl test: POST /api/ralph/start with project="." → 400 `{"error":"invalid project name"}` ✓
- `bun run scripts/gen-assets.ts` → regenerated public-assets.ts (no changes needed, already current)

## ALL SECURITY FIXES COMPLETE + VERIFIED

## Unit Tests: Escaping Functions — DONE
- Created `tests/unit/escaping.test.ts` with 41 tests across 4 describe blocks
- **xmlEsc** (10 tests): `& < > " '` → entities, passthrough for clean strings, empty string, combined entities, whitespace preservation
- **systemdEsc** (9 tests): backslash doubling, quote escaping, newline stripping, combined chars, consecutive backslashes, backslash-before-quote edge case
- **escAttr** (10 tests): quotes, backslashes, `<script>` XSS payload, null/undefined guards, angle brackets as hex, ampersand as hex, combined XSS vector
- **esc** (12 tests): HTML entities (`& < > " '`), null/undefined guards, HTML tag injection, combined entities, clean passthrough, empty string, number coercion
- Exported `xmlEsc` and `systemdEsc` from cli.ts (were module-private)
- `esc` and `escAttr` from index.html re-implemented as pure functions in test (browser DOM dependency)
- All 41 tests pass, cli.ts transpiles cleanly (231.77 KB bundled)

## Desktop Terminal: Phase 1a — WebSocket upgrade handler — DONE
- Added `ws` (v8.19.0) + `@types/ws` dependencies to package.json
- Added `import { WebSocketServer, type WebSocket } from "ws"` to serve.ts
- Created `ALLOWED_WS_KEYS` constant (mirrors existing HTTP key whitelist)
- Created `WebSocketServer` instance with `noServer: true` (attaches to existing http server)
- Implemented `handleTerminalWs(ws, session)`:
  - 100ms capture-pane polling loop, only sends on change (diff check via string equality)
  - Handles 3 message types: `input` (tmuxSend), `key` (tmuxSendKey with whitelist), `resize` (tmuxResize with bounds clamping)
  - Cleans up interval on close/error
- Added `server.on("upgrade")` handler:
  - Only accepts `/ws/terminal` path, destroys socket for anything else
  - Origin validation via existing `isAllowedOrigin()`
  - Session validation via existing `isAllowedSession()`
  - Hands off to `wss.handleUpgrade()` → `handleTerminalWs()`
- Transpiles cleanly with bun

## Desktop Terminal: Phase 2a — Load xterm.js via CDN — DONE (RE-IMPLEMENTED)
- NOTE: progress.txt previously claimed phases 2a-2e were done, but changes were lost (stashed/uncommitted)
- Re-implemented from scratch on clean working tree
- Added 4 CDN tags to `<head>` of `public/index.html` (before `</head>`):
  - `<link>` for `@xterm/xterm@5` CSS
  - `<script>` for `@xterm/xterm@5` main lib
  - `<script>` for `@xterm/addon-fit@0` (auto-resize to container)
  - `<script>` for `@xterm/addon-search@0` (in-terminal search)
- Added `<div id="xterm-container">` after existing `#terminal` div in terminal-view
- Added CSS for `#xterm-container` (display:none, flex:1, min-height:0, dark bg) and `.xterm` child (full height)
- Regenerated `public-assets.ts` via gen-assets.ts (6 files embedded)
- Build succeeds (178.77 KB bundled), all 410 tests pass

## Unit Tests: Validation Functions — DONE
- Created `tests/unit/validation.test.ts` with 77 tests across 4 describe blocks
- **isValidProjectName** (22 tests): valid names (alphanumeric, hyphens, dots, underscores, dotfiles, numeric start), rejects `.`, `..`, path traversal, empty, `<script>`, spaces, slashes, backslash, shell injection (`;`, `|`, backticks, `$()`), newlines, null bytes
- **CMD_REGEX** (18 tests): valid commands (paths, flags with `=`, spaces, dots/underscores/slashes), rejects shell metacharacters (`;`, `|`, `&&`, backticks, `$()`, `>`, `<`, `\`, quotes, `!`, newlines, empty)
- **isValidPlanFile** (19 tests): valid `.md` files (simple, spaces, hyphens, underscores, dots, numbers), rejects non-`.md`, no extension, `..`, path traversal, slashes, empty, bare `.md`, shell metacharacters, angle brackets, backticks, semicolons, pipes
- **BRANCH_REGEX** (18 tests): valid branches (simple, slashes, hyphens, underscores, dots, nested, numeric), rejects empty, spaces, shell metacharacters, backticks, `$()`, backslash, angle brackets, tilde, colon
- Validation functions replicated from serve.ts (not exported, same pattern as escaping.test.ts)
- All 77 tests pass in 7ms

## Unit Tests: Plan Parsing Functions — DONE
- Created `tests/unit/plan-parsing.test.ts` with 57 tests across 8 describe blocks
- **contentUsesCheckboxes** (10 tests): detects `- [ ]` and `- [x]` format, multiline plans, rejects section-based plans, empty string, plain text, non-checkbox bullets, indented checkboxes, partial syntax, missing dash prefix
- **extractCurrentTask (checkbox mode)** (6 tests): returns first unchecked task, skips checked, null when all checked, handles headers/descriptions before checkboxes, special chars in task text, checkbox priority over section headers
- **extractCurrentTask (section mode)** (12 tests): returns first `##`/`###` numbered section, skips `~~struck~~` headers, null when all struck, collects full section until next same-level header, stops at higher-level headers, includes subsections, paren numbering `1)`, lettered sub-numbering `1a.`, ignores unnumbered headers, ignores `####` headers, skips non-task content
- **extractCurrentTask (edge cases)** (3 tests): empty string → null, no-task plan → null, whitespace-only → null
- **parseSubtasks** (10 tests): extracts from `<subtasks>` block, missing block → empty array, empty block → empty array, trims whitespace, filters empty lines, multi-word tasks, single task, first block wins (non-greedy), special characters, empty string
- **countPlanTasks (checkbox mode)** (5 tests): all pending, all done, mixed, ignores non-checkbox lines, single task
- **countPlanTasks (section mode)** (8 tests): pending sections, struck-through as done, all struck, `###` headers, mixed `##`/`###`, ignores unnumbered headers, lettered sub-numbering, paren numbering
- **countPlanTasks (edge cases)** (3 tests): empty plan → {0,0}, no-task plan → {0,0}, plain text → {0,0}
- Functions replicated as pure functions (same pattern as other test files — not exported from source)
- All 57 tests pass in 10ms

## Unit Tests: Plan Mutation Functions — DONE
- Created `tests/unit/plan-mutation.test.ts` with 29 tests across 3 describe blocks
- Uses `tmpdir()` + `mkdtempSync` for isolated file I/O per test (cleaned up in `afterEach`)
- Functions replicated with configurable `planPath` param (originals use module-level `PLAN_PATH`)
- **markSectionDone** (10 tests): wraps `##` header in `~~`, wraps `###` header in `~~`, only strikes header not body, line-anchored (no partial match), no-op when header missing, no-op on empty taskText, handles regex-special chars `()[]`, only first match when duplicates exist, multiline taskText uses first line only, trailing whitespace handling
- **markCheckboxDone** (9 tests): `- [ ]` → `- [x]`, no-op on already-checked, exact match only (no substring), regex-special chars, no-op when task not found, line-anchored (no mid-line match), double-space mismatch, correct item among mixed checked/unchecked, empty taskText no-op
- **appendSubtasksToPlan** (10 tests): appends as unchecked checkboxes, strips `##`/`###` header prefixes, strips `~~` strikethrough, filters empty/whitespace subtasks, handles all-empty array, handles empty array, sanitizes combined header+strikethrough, trims whitespace, preserves existing plan content, regex-special chars pass through
- All 29 tests pass in 28ms

## Unit Tests: Ralph Log Parsing — DONE
- Created `tests/unit/ralph-log.test.ts` with 37 tests across 9 describe blocks
- Replicated `parseRalphLog()` and `countPlanTasks()` from serve.ts (module-private)
- Writes synthetic `.ralph.log` + plan files to tmpdir, cleaned up in afterEach
- **header parsing** (7 tests): agent, plan, progress, pid, started, totalIterations, project name extraction from directory path
- **iteration counting** (4 tests): Wax On format, old Iteration format, zero iterations, dynamic expansion (last match wins)
- **idle status** (2 tests): pid=0 not active, pid=1 guarded by pid>1
- **running status** (2 tests): active=true when PID alive (uses process.pid), active prevents completed even with finished line
- **dead PID** (3 tests): active=false, completed when all tasks done, not completed when tasks remain
- **stopped status** (1 test): finished line present with iteration limit reached, tasks still pending
- **cleanup phase** (3 tests): cleanup=true when Wax Off without complete + PID alive, cleanup=false when Wax Off complete, cleanup=false when PID dead
- **completed status** (4 tests): all checkboxes done, all sections struck through, no tasks → not completed, missing plan file → not completed
- **lastOutput** (4 tests): last 5 meaningful lines, skips === markers, skips header metadata, skips blank lines
- **finished timestamp** (2 tests): parses finished line, empty when not present
- **edge cases** (5 tests): empty log file, malformed header, header-only log, realistic full lifecycle
- All 37 tests pass in 25ms

## Unit Tests: Config Functions — DONE
- Created `tests/unit/config.test.ts` with 23 tests across 2 describe blocks
- Functions replicated with parameterized dependencies (same pattern as other test files)
- **remoteUrl** (6 tests): returns `https://hostname` when set, null when undefined, null when empty string, no trailing slash, hostname with port-like suffix, single-label hostname
- **isAllowedOrigin — localhost** (5 tests): allows `http://localhost:<PORT>`, allows `http://127.0.0.1:<PORT>`, rejects wrong port, rejects without port, rejects https localhost
- **isAllowedOrigin — tailnet** (7 tests): allows https on same tailnet, deeper subdomain, rejects http (non-https), rejects different tailnet, rejects substring-but-not-boundary match, rejects when suffix empty, allows tailnet origin with port
- **isAllowedOrigin — rejected** (5 tests): random external origin, empty string, garbage/non-URL, null-origin string, file:// origin
- All 23 tests pass in 8ms

## Snapshot Tests: Plist Generation — DONE
- Created `tests/snapshot/plist.test.ts` with 25 tests across 5 describe blocks
- Replicated `generatePlist()` as pure function (parameterized: config, args, logPath)
- **snapshot** (4 tests): default config, null config, tailscale config, single-arg (compiled binary)
- **homebrew paths** (4 tests): /opt/homebrew/bin present, /opt/homebrew/sbin present, full PATH string in correct order, PATH wrapped in `<string>` tags
- **XML validity** (7 tests): balanced tags (default config), balanced tags (null config), balanced tags (with env vars), XML declaration present, DOCTYPE present, matching `<plist>` root, no self-closing tags where content expected
- **environment variables** (5 tests): WOLFPACK_DEV_DIR present, WOLFPACK_PORT present, omits env vars when null config, port 0 is falsy (omitted), XML-special chars escaped in devDir
- **structure** (5 tests): label is com.wolfpack.server, RunAtLoad true, KeepAlive true, log paths present (stdout before stderr), ProgramArguments contains all args
- Includes `xmlTagsBalanced()` helper for naive XML validation (checks open/close tag pairing)
- All 25 tests pass in 12ms, 4 snapshots generated

## Integration Tests: API Endpoints — DONE
- Created `tests/integration/api.test.ts` with 49 tests across 10 describe blocks
- Spins up real `node:http` server on random port (port 0), tears down in afterAll
- Mocks tmux functions via `bun:test` mock(): tmuxList, capturePane, tmuxSend, tmuxSendKey, tmuxResize, tmuxNewSession, tmuxKillSession
- Replicates route handler logic from serve.ts (same pattern as unit tests — private functions replicated)
- **GET /api/info** (2 tests): returns name + version, correct content-type
- **GET /api/sessions** (3 tests): returns session list with lastLine, empty list when no sessions, capturePane called per session
- **POST /api/send** (5 tests): valid send, noEnter flag passthrough, missing session/text → 400, unknown session → 404
- **POST /api/create** (8 tests): valid project, unique name on collision, invalid/empty/dot-dot project → 400, missing project → 400, newProject field, cmd passthrough
- **POST /api/key** (7 tests): allowed key, all whitelisted keys pass, disallowed key → 400, arbitrary string → 400, unknown session → 404, missing session/key → 400
- **POST /api/kill** (3 tests): valid kill, unknown session → 404, missing session → 400
- **POST /api/resize** (9 tests): valid resize, clamp cols min/max (20/300), clamp rows min/max (5/100), missing params → 400, unknown session → 404, boundary values (20×5, 300×100)
- **bad JSON body** (3 tests): unparseable JSON → 400 "invalid JSON body", empty body → 400, truncated JSON → 400
- **body > 64KB** (2 tests): oversized body rejected (connection reset or error), body just under 64KB accepted
- **CORS** (5 tests): allowed origin gets headers, rejected origin → 403, no origin → no CORS headers, OPTIONS preflight with allowed origin → 204, OPTIONS preflight with rejected origin → 403
- **unknown routes** (2 tests): GET unknown → 404, POST to GET-only → 404
- All 49 tests pass in 33ms

## Snapshot Tests: Systemd Unit Generation — DONE
- Created `tests/snapshot/systemd.test.ts` with 30 tests across 4 describe blocks
- Replicated `generateSystemdUnit()` as pure function (parameterized: config, args)
- Replicated `systemdEsc()` helper for escaping systemd Environment values
- **snapshot** (4 tests): default config, null config, single-arg (compiled binary), tailscale config
- **ExecStart quoting** (6 tests): each arg double-quoted, single arg quoted, spaces in args, backslashes escaped (doubled), quotes escaped (backslash-quote), newlines stripped
- **Environment escaping** (10 tests): backslash escaping in devDir, quote escaping in devDir, newline stripping in devDir, combined escaping, port as unquoted number, omits env when null config, port 0 falsy (omitted), PATH always present, PATH line unquoted
- **structure** (10 tests): [Unit]/[Service]/[Install] sections present and ordered, Description, After=network.target, Type=simple, Restart=always + RestartSec=5, WantedBy=default.target, ends with newline
- All 30 tests pass in 8ms, 4 snapshots generated

## Integration Tests: Ralph API Endpoints — DONE
- Created `tests/integration/ralph-api.test.ts` with 42 tests across 5 describe blocks
- Uses real `node:http` server on random port, temp directory for fake DEV_DIR
- Filesystem-based testing: creates real project dirs, plan files, and .ralph.log files per test
- Fake spawn/exec/kill tracking (no real processes spawned or killed)
- **POST /api/ralph/start** (12 tests): valid params spawn worker with correct args, defaults (5 iterations, claude agent), iteration clamping (min 1, max 50), format flag passthrough, invalid project → 400, dot-dot → 400, missing project → 400, project dir not found → 404, missing plan → 404, custom plan not found → 404, invalid plan name (path traversal) → 400, active loop conflict → 409
- **POST /api/ralph/cancel** (7 tests): PID verification via ps, SIGTERM to process + process group, rejects non-ralph PID → 400, ps throws → 404, no active loop → 404, invalid project → 400, missing project → 400
- **POST /api/ralph/dismiss** (7 tests): plan deleted + verified on disk, path traversal in log planFile → 400, active loop → 409, no plan in log → 404, plan already deleted → 404, invalid project → 400, no ralph log → 404
- **GET /api/ralph** (5 tests): scans projects with logs, skips projects without logs, skips projects where plan file missing, empty when no projects, includes task counts
- **GET /api/ralph/task-count** (10 tests): checkbox-style counting, section-style counting, all done, empty plan → {0,0}, plan not found → 404, invalid project → 400, missing project → 400, invalid plan → 400, missing plan → 400, custom plan file name
- All 42 tests pass in 41ms

## Cleanup Pass — DONE
- Reviewed all files changed in last 10 commits + uncommitted changes
- **Dead code found**: unused `spyOn` import in `tests/integration/ralph-api.test.ts` — removed
- **No other dead code**: all functions, exports, imports, types, and interfaces across cli.ts, serve.ts, ralph-macchio.ts, public-assets.ts, and all 10 test files are actively used
- All 387 tests pass after cleanup

## Phase 1a: WebSocket Upgrade Handler — DONE
- Added `ws` npm dependency (v8.19.0)
- Imported `WebSocketServer` and `WebSocket` type from `ws`
- Created `WebSocketServer` instance in `noServer` mode
- Implemented `handleTerminalWs(ws, session)` function:
  - 100ms capture-pane polling loop, sends `{ type: "output", data }` on change
  - Handles incoming messages: `input` (tmuxSend), `key` (tmuxSendKey), `resize` (tmuxResize)
  - Cleans up interval on close/error
- Added `server.on("upgrade")` handler at `/ws/terminal?session=X`:
  - Validates session via `isAllowedSession()` — returns 403 if invalid
  - Returns 404 for non-terminal upgrade paths
- All 410 tests pass, build succeeds

## Phase 1b: handleTerminalWs — ALREADY DONE (no-op)
- Task 1b requested but implementation already exists at serve.ts:1002-1047 (commit 0eefa31)
- Verified: all spec points implemented (session validation, 100ms polling, output/input/key/resize messages, close cleanup)
- Build passes, all 410 tests pass

## Phase 2a: Load xterm.js via CDN — DONE (re-implemented, previous changes were lost)
- Added 4 CDN tags to `<head>` of `public/index.html` (before `</head>`):
  - `<link>` for `@xterm/xterm@5` CSS
  - `<script>` for `@xterm/xterm@5` main library
  - `<script>` for `@xterm/addon-fit@0` (auto-resize to container)
  - `<script>` for `@xterm/addon-search@0` (in-terminal search)
- Added `<div id="xterm-container">` after existing `#terminal` div in terminal-view
- Added CSS for `#xterm-container` (display:none, flex:1, min-height:0, dark bg) and `.xterm` child (full height)
- Regenerated `public-assets.ts` via gen-assets.ts (6 files embedded)
- All 410 tests pass

## Phase 2b: Desktop terminal element — ALREADY DONE (no-op)
- Task requested `<div id="xterm-container" style="display:none"></div>` alongside existing terminal
- Element already exists at `public/index.html:876`: `<div id="xterm-container"></div>`
- `display:none` already applied via CSS rule at line 770-775: `#xterm-container { display: none; ... }`
- No changes needed — functionally identical to spec

## Phase 2c: Desktop detection & switching — DONE
- Added global state vars: `xtermInstance`, `xtermFitAddon`, `xtermWs`, `useDesktopTerminal`
- Added `isDesktop()` helper: `!('ontouchstart' in window) && window.innerWidth > 768`
- Added `initXterm()` function:
  - Hides mobile UI (`#terminal`, `#action-bar`, `#input-bar`, `#msg-preview`)
  - Shows `#xterm-container` with `display: flex`
  - Creates xterm.js Terminal with dark theme, SF Mono font, cursor blink
  - Loads FitAddon, opens in container, auto-fits
  - Connects WebSocket to `/ws/terminal?session=X` (proto-aware: ws/wss)
  - Sends initial resize dimensions on ws.open
  - Handles `output` messages with `term.reset()` + `term.write()`
  - Shows reconnecting status on ws.close
  - Forwards xterm `onData` → ws `input` messages
  - Window resize handler: re-fits terminal + sends new dimensions via ws
- Added `destroyXterm()` function:
  - Closes WebSocket, disposes xterm instance, removes resize listener
  - Clears container innerHTML, restores mobile element display defaults
- Modified `openSession()`: branches on `isDesktop() && !machineUrl` — desktop uses initXterm(), mobile uses existing HTTP polling
- Modified `switchSession()`: tears down current mode (destroyXterm or stopPolling), re-evaluates desktop status, initializes appropriate mode
- Modified back button in `showView("terminal")`: calls destroyXterm() or stopPolling() based on mode
- Modified `closeSession()`: same desktop/mobile teardown branching
- Modified `createSessionWithAgent()`: sets useDesktopTerminal flag, branches init
- Desktop mode only activates for local sessions (not remote/machineUrl) since WebSocket endpoint is local-only

## Phase 2d: xterm.js initialization — ALREADY DONE (no-op)
- Task 2d requested initXterm() implementation, but it already exists at public/index.html:1734-1806 (commit 568b7dd)
- Verified all spec points implemented:
  - Terminal config: cursorBlink, fontSize 13, SF Mono/Fira Code, dark theme ✓
  - term.open(#xterm-container) ✓
  - WebSocket connection to /ws/terminal?session=X ✓
  - server→xterm: ws.onmessage → term.reset() + term.write() ✓
  - xterm→server: term.onData → ws.send({type:"input"}) ✓
  - resize: term resize → ws.send({type:"resize"}) + window resize → fitAddon.fit() ✓
  - FitAddon loaded for auto-sizing ✓
- Additional features beyond spec: ws/wss protocol detection, initial resize on open, destroyXterm() cleanup, reconnect status indicator
- All 410 tests pass

## Phase 2e: Output strategy (reset+write) — ALREADY DONE (no-op)
- Task 2e described the output strategy: send full capture-pane output, use `term.reset()` + `term.write()` on each update
- Server side already implements this at `serve.ts:1002-1047`:
  - 100ms polling via `capturePane(session)`
  - Diff check: only sends when `pane !== prev` (string equality)
  - Sends `{ type: "output", data: pane }` with full pane content
- Client side already implements this at `public/index.html:1773-1781`:
  - `ws.onmessage` handler: `term.reset()` then `term.write(msg.data)`
- No changes needed — strategy fully implemented in commits 0eefa31 + 568b7dd

## Phase 2f: Search on desktop (xterm SearchAddon) — DONE
- SearchAddon CDN script was already loaded (`@xterm/addon-search@0`) but never instantiated
- Added `xtermSearchAddon` global variable alongside existing `xtermInstance`, `xtermFitAddon`, `xtermWs`
- In `initXterm()`: instantiate `new SearchAddon.SearchAddon()`, load onto terminal via `term.loadAddon()`
- In `destroyXterm()`: null out `xtermSearchAddon` reference (disposed with terminal)
- Modified search functions to branch on `useDesktopTerminal && xtermSearchAddon`:
  - `doSearch()`: calls `xtermSearchAddon.findNext(searchTerm)` on desktop, falls back to DOM regex on mobile
  - `nextMatch()`: calls `xtermSearchAddon.findNext(searchTerm)` on desktop
  - `prevMatch()`: calls `xtermSearchAddon.findPrevious(searchTerm)` on desktop
  - `closeSearch()`: calls `xtermSearchAddon.clearDecorations()` on desktop, restores `textContent` on mobile
- Search bar UI (input, prev/next/close buttons) unchanged — works as overlay for both modes
- DOM-based search preserved for mobile (no xterm instance on mobile)
- Regenerated `public-assets.ts` (6 files embedded)
- All 410 tests pass

## Cleanup Pass (desktop terminal session) — DONE
- Reviewed all files changed in last 10 commits + uncommitted changes
- **Dead code found**: unused `spyOn` import in `tests/integration/ralph-api.test.ts` — removed (again; previous cleanup didn't persist)
- **No other dead code**: all functions, exports, imports, types across serve.ts, cli.ts, ralph-macchio.ts, public/index.html, public-assets.ts, and all 10 test files are actively used
- **Cross-file exports verified**: all exports (xmlEsc, systemdEsc from cli.ts; assets from public-assets.ts; printQR from qr.ts) have consumers
- All 410 tests pass after cleanup

## Replace xterm.js CDN with ansi_up — DONE
- Removed 4 xterm.js CDN tags from `public/index.html`:
  - `<link>` for `@xterm/xterm@5` CSS
  - `<script>` for `@xterm/xterm@5` main lib
  - `<script>` for `@xterm/addon-fit@0`
  - `<script>` for `@xterm/addon-search@0`
- Added single ansi_up CDN script: `https://cdn.jsdelivr.net/npm/ansi_up@6/ansi_up.min.js` (~5KB vs ~200KB+ for xterm)
- Replaced xterm-specific CSS rules (`.xterm`, `.xterm-cursor-layer`) with `#xterm-container pre` styling
- Kept `#xterm-container` base CSS (still needed for desktop terminal container)
- Regenerated `public-assets.ts` (6 files embedded)
- All 410 tests pass

## Replace #xterm-container with styled `<pre>` — DONE
- Replaced `<div id="xterm-container">` with `<pre id="desktop-terminal">` in HTML
- Replaced `#xterm-container` CSS rules with `#desktop-terminal` styled `<pre>`:
  - `background: #0a0a0a`, `color: #e0e0e0` (dark theme)
  - `font-family: "SF Mono", "Menlo", "Consolas", "Liberation Mono", monospace`
  - `font-size: 13px`, `line-height: 1.4`, `padding: 4px 8px`
  - `overflow-y: auto` for scrollback
  - `white-space: pre` to preserve spacing
  - `user-select: text` + `-webkit-user-select: text` for copy support
  - `display: none` by default (hidden on mobile, shown via JS on desktop)
  - `flex: 1`, `min-height: 0` for flexbox layout
- Removed old `#xterm-container pre` sub-rule (no longer needed — element IS the `<pre>`)
- Updated both JS references: `getElementById("xterm-container")` → `getElementById("desktop-terminal")` in `initXterm()` and `destroyXterm()`
- Zero `xterm-container` references remain in codebase
- Regenerated `public-assets.ts` via build script
- All 410 tests pass

## Replace `initXterm()` with `initDesktopTerminal()` — DONE
- Replaced `initXterm()` with `initDesktopTerminal()` using AnsiUp instead of xterm.js
- New function:
  - Shows `#desktop-terminal` (`<pre>`), hides mobile elements (action-bar, input-bar, msg-preview, #terminal)
  - Creates `AnsiUp` instance with `use_classes: false` (inline styles)
  - Opens WebSocket to `/ws/terminal?session=X` (protocol-aware: ws/wss)
  - On open: calculates cols/rows via `calcTermDimensions()` (probe span for char metrics), sends resize message
  - On message: converts ANSI→HTML via `ansi_up.ansi_to_html()`, sets `pre.innerHTML` (atomic, no flicker)
  - On close: shows connection status banner
  - Keyboard listener: maps key events to terminal escape sequences (arrows, ctrl+keys, pgup/pgdn, etc.), sends as `input` messages
  - Window resize listener: recalculates cols/rows, sends resize via WebSocket
- Extracted `calcTermDimensions(pre)` helper (same font-probe approach as `resizePane()`, matched to 13px/1.4 line-height)
- Replaced `destroyXterm()` with `destroyDesktopTerminal()`:
  - Closes WebSocket, removes keydown + resize listeners, clears `<pre>` content, restores mobile elements
- Removed all xterm.js state vars (`xtermInstance`, `xtermFitAddon`, `xtermSearchAddon`, `xtermWs`)
- Added new state vars: `desktopAnsiUp`, `desktopWs`, `desktopResizeHandler`
- Updated all 5 call sites: openSession, switchSession, back button, closeSession, createSessionWithAgent
- Simplified search functions: removed `xtermSearchAddon` branches (desktop terminal is a `<pre>` DOM node, so DOM-based search from mobile works for both — search will be addressed separately in Phase 2)
- Regenerated `public-assets.ts`
- All 410 tests pass

## Replace `destroyXterm()` with `destroyDesktopTerminal()` — ALREADY DONE (no-op)
- Task 4 requested but `destroyDesktopTerminal()` already exists at `public/index.html:1837-1858`
- Implemented as part of the `initDesktopTerminal()` refactor (previous iteration)
- Verified all 5 spec points:
  1. Close WebSocket — `desktopWs.close()` ✓
  2. Remove keyboard listener — `removeEventListener("keydown", pre._onKeyDown)` ✓
  3. Remove resize listener — `removeEventListener("resize", desktopResizeHandler)` ✓
  4. Hide `#desktop-terminal`, restore mobile elements — `pre.style.display = "none"` + restores terminal/action-bar/input-bar ✓
  5. Clear innerHTML — `pre.innerHTML = ""` ✓
- Zero references to `destroyXterm` remain in code (only in plan doc)
- 3 call sites wired: back button (line 1115), switchSession (line 2034), closeSession (line 2401)
- Struck through plan item §4 in `desktop_terminal_plan.md`
- All 410 tests pass

## Update all callsites (task 5) — ALREADY DONE (no-op)
- Task 5 requested updating all callsites from xterm → desktop terminal naming
- Grep confirms ZERO references to `initXterm`, `destroyXterm`, `xtermInstance`, `xtermFitAddon`, `xtermSearchAddon`, `xtermWs` in any .ts/.js/.html file
- All migrations were completed in commit 182a1e4 (replace initXterm with initDesktopTerminal)
- Current state: `desktopWs`, `desktopAnsiUp`, `desktopResizeHandler` as globals, `initDesktopTerminal()` + `destroyDesktopTerminal()` at all 5+ call sites
- Struck through plan item §5 in `desktop_terminal_plan.md`
- All 410 tests pass

## Task 6: Direct keyboard input handler — DONE
- Refactored `onKeyDown` handler in `initDesktopTerminal()` (public/index.html:1793-1830)
- **Focus guard**: skip capture when `INPUT`, `TEXTAREA`, `SELECT`, or `contentEditable` element is focused
- **Ctrl+key combos**: send as `{ type: "key", key: "C-x" }` tmux notation (covers C-c, C-d, C-z, C-l, C-a, C-e and all other ctrl+letter combos)
- **Special keys via keyMap**: `Enter`, `Escape`, `BSpace`, `Tab`, `DC`, `Up`/`Down`/`Left`/`Right`, `Home`, `End`, `PPage`, `NPage` — sent as `{ type: "key", key: "..." }`
- **Printable chars**: sent as `{ type: "input", data: char }` (unchanged)
- **preventDefault()**: applied on all captured keys to suppress browser defaults
- Server already handles both `type: "input"` (tmuxSend with -l) and `type: "key"` (tmuxSendKey without -l) — no server changes needed
- Regenerated `public-assets.ts`
- All 410 tests pass
