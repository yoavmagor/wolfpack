# Progress Log

## 2026-02-06: Phase 1.1 — bun compat audit (COMPLETE)
- audited all 6 node: modules used across codebase — all supported in bun ≥1.2
- qrcode-terminal: pure JS, no native deps, will bundle cleanly
- import.meta.dirname: CONFIRMED returns /$bunfs/root/ in compiled binaries (by design)
  - 4 usages identified that need migration (2 in serve.ts, 2 in cli.ts)
  - serve.ts PUBLIC_DIR → solved by asset embedding (phase 1.2)
  - serve.ts SETTINGS_PATH → needs relocation to ~/.wolfpack/
  - cli.ts plist/systemd refs → solved by process.execPath (phase 1.4)
- execFileSync/execFile(promisified): fully supported, no issues
- node:fs/promises dynamic import (serve.ts:490): supported
- updated PLAN.md with detailed audit results

## 2026-02-06: Phase 1.2 — embed public/ assets (COMPLETE)
- created `scripts/gen-assets.ts` — scans `public/` and generates `public-assets.ts`
- text files (html, json) embedded as JSON-escaped strings
- binary files (png) embedded as base64, decoded to Uint8Array at import time
- generated module exports `Map<string, { content: string | Uint8Array; mime: string }>`
- verified: all 6 assets load correctly, binary sizes match originals exactly
- also created `scripts/bun-compat-test.ts` (24 tests, all pass)

## 2026-02-06: Phase 1.3 — refactor serve.ts (COMPLETE)
- imported embedded `assets` map from `public-assets.ts` into `serve.ts`
- rewrote `serveFile()` to look up from assets map instead of `readFileSync(join(PUBLIC_DIR, ...))`
- rewrote manifest.json route to parse from embedded asset (`assets.get("manifest.json")`)
- rewrote static file fallback to serve from embedded assets map (no more `fs.stat` + disk reads)
- removed `PUBLIC_DIR` constant entirely
- removed `import.meta.dirname` usage for public dir (only remains for SETTINGS_PATH — phase 1.4)
- removed unused `basename` import from `node:path`
- cleaned dead code in sw.js route (unreachable `readFileSync` block after early return)
- removed dynamic `import("node:fs/promises")` — no longer needed
- verified: all 6 assets still load correctly via import, bun compat tests pass (24/24)

## 2026-02-06: Phase 1.4 — refactor cli.ts service generation (COMPLETE)
- rewrote `generatePlist()`: ProgramArguments now uses `process.execPath` (the compiled binary) instead of tsx/serve.ts
- rewrote `generateSystemdUnit()`: ExecStart now uses `process.execPath` instead of tsx/serve.ts
- removed all `import.meta.dirname` usage from cli.ts (was used for tsx/serve.ts paths)
- removed all `tsx` and `node_modules` references from service generation
- simplified PATH env in both plist and systemd unit — no longer includes node's bin dir
- removed `checkNodeVersion()` function entirely — no longer needed with binary distribution
- removed `node` from missing deps check in `setup()`
- removed `nodejs` from apt package map
- removed `npm unlink -g wolfpack` from `uninstall()` — no npm in binary world
- updated uninstall messages to reference binary path instead of npm global command
- updated shebang from `#!/usr/bin/env -S npx tsx` to `#!/usr/bin/env bun`
- removed unused `execFileSync` import
- verified: no `import.meta.dirname` or `tsx` references remain in cli.ts, file parses cleanly

## 2026-02-06: Phase 1.5 — qr.ts verification (COMPLETE)
- tested `qrcode-terminal` under bun runtime: works perfectly
- tested `qrcode-terminal` under `bun build --compile`: bundles and runs correctly (12 modules bundled)
- QR output renders correctly from compiled binary
- pure JS package, zero native deps — no vendoring or inlining needed
- decision: leave as-is, bun handles it automatically
- phase 1 is now FULLY COMPLETE

## 2026-02-06: Phase 2.1 — build script (COMPLETE)
- created `scripts/build.ts` — orchestrates full build pipeline
  - step 1: runs `scripts/gen-assets.ts` to generate embedded assets
  - step 2: creates `dist/` directory
  - step 3: compiles for all 4 targets (linux-x64, linux-arm64, darwin-x64, darwin-arm64)
- `dist/` already in `.gitignore` (added in earlier phase)
- tested: all 4 binaries compile successfully
  - darwin-arm64: 57MB, darwin-x64: 62MB, linux-arm64: 92MB, linux-x64: 97MB
- verified native binary (darwin-arm64) runs correctly — shows help/logo output
- 15 modules bundled per target

## 2026-02-06: Phase 2.2 — GitHub Actions release workflow (COMPLETE)
- created `.github/workflows/release.yml`
- triggers on tag push matching `v*`
- uses `oven-sh/setup-bun@v2` to install latest bun
- runs `bun install` then `bun run scripts/build.ts` to build all 4 targets
- generates SHA256 checksums file (`checksums-sha256.txt`) in dist/
- creates GitHub Release with auto-generated release notes via `softprops/action-gh-release@v2`
- uploads all 4 binaries + checksums as release assets
- phase 2 is now FULLY COMPLETE

## 2026-02-06: Phase 3.1 — rewrite install.sh (COMPLETE)
- completely rewrote `install.sh` to download pre-built binary instead of git clone + npm install
- `detect_target()` function maps `uname -s` + `uname -m` to binary name (e.g. `wolfpack-darwin-arm64`)
- downloads from `https://github.com/almogdepaz/wolfpack/releases/latest/download/<target>`
- supports both curl and wget for download
- installs binary to `~/.wolfpack/bin/wolfpack`, chmod +x
- symlinks to `/usr/local/bin/wolfpack` for PATH access (with sudo fallback, manual instructions if both fail)
- removed ALL node/npm/git-clone logic (no more `npm install`, `npm link`, `git clone`)
- removed node.js version check from prerequisites
- kept tmux + tailscale as system deps (unchanged install logic for those)
- kept wolf ASCII art banner
- still runs `wolfpack setup` at end (with fallback to direct binary path if not on PATH yet)
- bash syntax check passes

## 2026-02-06: Phase 4.1 — remove package-lock.json (COMPLETE)
- removed `package-lock.json` from git tracking (`git rm -f`)
- added `package-lock.json` to `.gitignore` to prevent re-addition
- bun uses `bun.lock` instead; npm lockfile is no longer needed

## 2026-02-06: Phase 4.1 — remove tsx dependency references (COMPLETE)
- removed `tsx` from `package.json` dependencies
- removed `engines.node` constraint from `package.json` (no longer relevant for binary distribution)
- updated `serve.ts` shebang from `#!/usr/bin/env -S npx tsx` to `#!/usr/bin/env bun`
- updated `serve.ts` usage comment from `npx tsx serve.ts` to `bun serve.ts`
- verified: no `tsx` references remain in source files (only in `.claude/settings.local.json` which is not project code)
- `bun install` succeeds, tsx package removed from node_modules
- gen-assets and cli.ts parse/build correctly

## 2026-02-06: Phase 4.1 — simplify package.json (COMPLETE)
- removed `"bin"` field from `package.json` — no longer relevant with binary distribution (no npm link/npx)
- kept `name`, `version`, `type`, `description`, `dependencies` — all still needed for bun's package management
- `qrcode-terminal` dependency stays — it's a real npm dep used by `qr.ts`, resolved by bun at build time
- decided against `bunfig.toml` — bun works fine with `package.json` defaults, no custom config needed
- verified: `bun install` succeeds, no references to removed `"bin"` field anywhere in codebase
- phase 4.1 is now FULLY COMPLETE

## 2026-02-06: Phase 4.2 — generated file management (COMPLETE)
- confirmed `public-assets.ts` is already gitignored (added in earlier phase)
- confirmed file is not tracked by git
- confirmed `scripts/build.ts` runs gen-assets as step 1, so the file is always regenerated before builds
- decision: gitignore (generated at build time), no need to commit for dev convenience
- phase 4.2 is COMPLETE

## 2026-02-06: Phase 4.3 — dev workflow verification (COMPLETE)
- verified `bun run cli.ts` works for local dev (server starts, fails only due to port already in use)
- verified `bun run scripts/build.ts` produces all 4 binaries successfully:
  - linux-x64, linux-arm64, darwin-x64, darwin-arm64
  - 15 modules bundled per target
- dev workflow requires only bun installed — no node/npm needed
- README documentation deferred to phase 4.4

## 2026-02-06: Phase 4.4 — update README (COMPLETE)
- replaced "Quick Install" description: now says it downloads a pre-built binary (not clone+npm)
- removed "Manual Install" section entirely (git clone + npm install + npm link flow is dead)
- removed Node.js from Prerequisites — only tmux + Tailscale remain
- added "No Node.js or npm required" note under prerequisites
- updated Setup Wizard step 1 to say "tmux, Tailscale" instead of "Node.js, tmux, Tailscale"
- updated bridge-settings.json config reference (no longer "in the app directory")
- added "Building from Source" section: bun install, bun run scripts/build.ts, dev mode via bun run cli.ts
- added supported platforms line (macOS Apple Silicon/Intel, Linux x64/arm64)
- phase 4.4 is COMPLETE — all sub-tasks in PLAN.md are now checked off
- ALL PHASES COMPLETE (1.1–4.4)
